# 修复（故事 02 第 2 轮 → 第 3 轮）

每条对应 `归因.md` 的证据。制作人三条指示不变。

## 字段预算

| | 顶层 | 子阶段 | 拍 | 选项 | 接线条目 | 玩法 | 地图汇总 | 合计 |
|---|---|---|---|---|---|---|---|---|
| 上一版 | 6 | 23 | 22 | 6 | 2 | 8 | 1 | 68 |
| 这一版 | 7 | 24 | 22 | 6 | 2 | 8 | 1 | **70** |

+2：顶层和子阶段各一格「红线（不许做什么）」，纯文本，缺省空。理由：A 日志 #12「没地方写」——采阳段的红线（不演肉体、不出血浆、无音乐无惨叫）只能塞进氛围格；判定书另记两条整段纪律（「七道」这个词游戏内不出现、E5 不设 Game Over）整条丢了。这是交接必须带走的东西，没有格子就会丢。方法依据：交接文档一向有「约束／禁止」一栏（Lemarchand 的 macro 之外配 design constraints；任天堂的关卡文档写「不做什么」）。

## 修复清单

1. **两个「点了没反应」**（#16、#17）。「＋ 要先发生过的事」原来藏在折叠块里，自动化浏览器找得到点不到——改成不折叠。所有「删除」改成页内确认框（原生 confirm 会被自动化浏览器静默取消；两轮 A 都报了；手机上也更一致）；没内容的拍直接删不问。
2. **文案八处**（#4 #5 #6 #7 #10 #11 #13；B-1）：必经／可选说的是这一段本身；顶层与子阶段「关二狗的目的」提示分层；设计目的归纳只说这一段独有的；四格「想过了确实没有写无」；情绪轴量松紧好坏不量刺激；有条件才出现的做法把条件写在括号里（不加字段）；「玩家知道什么」写玩家与关二狗之间的差；「整段随时可发生」的说明加「到点自动发生的事（天亮、鸡叫）也写一拍，每条出路都得有拍或选项接着」。
3. **开始／结束局面旁显示对照**（B-3、B-4；A「同一件事写三遍会漂」）：「开始时局面」旁显示第一拍的发生，「结束时局面」旁显示最后一拍的结果，并提醒「两边说的账（火几格、身上什么）要一样」。不加字段，只是把要对上的两头摆到一起。
4. 同 2 末条（B-1）。
5. **红线**（#12；判定书纪律遗失）：顶层、子阶段各一格；导出全文、CLI 都印。

## 没改的

- #1（E0 算不算出去时）、#15（一拍一拍加）：内容取舍／方法本身，留观。
- #3、#8、#14、#18：使用侧（光标不在输入框、LLM 错字、引用过期）。
- B-2、B-3、B-4、B-5：作者写岔的账，工具不替他算；修复 3 只是把两头摆一起。

## 存档说明

本目录的 `度量.json` 等三份在修完后重导过，`fieldBudget.合计` 是 70（判定时看到的是 68）；其余内容不变。
